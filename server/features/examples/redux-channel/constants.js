const ONLINE = `ONLINE`;
const OFFLINE = `OFFLINE`;
const AWAY = `AWAY`;
const FETCHED = `FETCHED`;
const NOT_FETCHED = `NOT_FETCHED`;
const Chance = require('chance');
const chance = new Chance();

module.exports = {
    ONLINE, OFFLINE, AWAY, FETCHED, chance
}